{
    "name": "model/jwt",
    "description": "JWT bridge for ModEl Framework",
    "type": "library",
    "authors": [
        {
            "name": "Domenico Giambra",
            "email": "zorinik@gmail.com"
        }
    ],
    "autoload": {
        "psr-4": {"Model\\JWT\\": "src/"}
    },
    "require": {
        "model/config": "^0.6.0",
        "firebase/php-jwt": "^6.1",
        "composer-runtime-api": "^2.1"
    },
    "suggest": {
        "model/cache": "If you want to store the secret key in the filesystem",
        "model/redis": "If you want to store the secret key in redis"
    }
}
